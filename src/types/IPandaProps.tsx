// interface for the props of the Panda component
export interface IPandaProps {
    // Title of the page
    title?: string;
    // Background color of the page
    backgroundColor?: string;
    // Link to the page
    link?: string;
}